---
description: Git 提交说明必须使用中文
alwaysApply: true
---

# 中文提交说明（全局强制）

生成任何 `git commit` 消息时，**必须**使用简体中文，面向审阅者说明「改了什么 / 为什么改」。

## 格式

```
<一行摘要>

<可选正文：动机、影响范围、验证要点>
```

## 要求

1. 标题与正文均使用标准简体中文；专有名词、文件名、API、命令可保留英文原文。
2. 标题用陈述句概括变更，避免空泛词（如「更新代码」「fix」）。
3. 涉及电气/算法语义时，在正文点明物理或工程意图（例如：单端波头卡尺、三相切换显示）。
4. 禁止仅用英文 Conventional Commit（如 `feat:` / `fix:`）作为唯一说明；若需前缀，中文摘要仍须完整可读。
5. 一次性提交多种改动时，正文用条目列出要点。

## 示例

```
将标注工具改为 ABC 单相切换显示，并接入应用图标

- 主图区同一时刻仅渲染活动相，底部保留三相标注面板
- 开发运行与 PyInstaller 打包共用 assets 中的 app_icon
```
